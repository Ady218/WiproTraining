"""

"""
from my_pack.basic_shapes import area_of_square, peri_of_square, area_of_rect
from my_pack.circle import area_of_circle, peri_of_circle  # directory name, module name, function name

radius = int(input('Enter Radius: '))

print('Area: ',area_of_circle(rad=radius))
print('Perimeter: ',peri_of_circle(rad=radius))# we can use positional parameters but keyword parameter is to be practiced

si = int(input('side of square: '))
print('Area: ', area_of_square(side=si))
print('Peri: ', peri_of_square(side=si))

l = int(input('length of rect: '))
b = int(input('breadth of rect: '))
print('Area of rect: ', area_of_rect(l,b))

# in business functionality is written separately and function calling drivers are written separately.


